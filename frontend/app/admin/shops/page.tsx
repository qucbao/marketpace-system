import { RoutePlaceholder } from "@/components/route-placeholder";

export default function AdminShopsPage() {
  return (
    <RoutePlaceholder
      eyebrow="Admin"
      title="Admin Shops"
      description="Admin moderation route scaffold for approving and rejecting shop registrations."
      path="/admin/shops"
    />
  );
}

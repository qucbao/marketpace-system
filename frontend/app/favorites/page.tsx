import { RoutePlaceholder } from "@/components/route-placeholder";

export default function FavoritesPage() {
  return (
    <RoutePlaceholder
      eyebrow="User"
      title="Favorites"
      description="Saved product route scaffold for the favorite items flow."
      path="/favorites"
    />
  );
}
